# Binaries (root-level compiled binary only, not cmd/synapse source dir)
/synapse
/synapse-*
*.exe

# Env / secrets
.env
.env.*
!.env.example

# Vector DB (local data)
*.db
*.db-shm
*.db-wal

# Go
/vendor/

# SDD / tooling internals (local only)
.atl/
.engram/

# OS
.DS_Store
