# Binaries (root-level compiled binary only, not cmd/synapse source dir)
/synapse
/synapse-*
*.exe

# Env / secrets
.env
.env.*
!.env.example

# Vector DB (local data)
*.db
*.db-shm
*.db-wal

# Go
/vendor/

# SDD / tooling internals (local only — contain local paths, not for publishing)
.atl/
.engram/
openspec/

# OS
.DS_Store
